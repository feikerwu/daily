const levelOrder = function(root) {
		let res = new Array()
		let curLevel = [root]
		let nextLevel = new Array()
		let nextLevelVals = new Array()
		res.push([root.val])
		while (curLevel.length) {
				
		}
}

